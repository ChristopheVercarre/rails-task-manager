<h1>Overview Tasks</h1>

<ul>
<% @tasks.each_with_index do |task, index| %>
<li><%= index + 1 %> - <%=task.name %> <%= link_to 'Details', task_path(task) %> <%= link_to 'Edit', edit_task_path(task) %> <%= link_to 'Delete', task_path(task), method: :delete %>
</li>
<% end %>
</ul>

<%= link_to 'Create a new task', new_task_path %>


